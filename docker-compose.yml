services:
  qdrant:
    image: qdrant/qdrant:v1.18.2
    container_name: visual-search-qdrant
    restart: unless-stopped

    ports:
      - "6333:6333"
      - "6334:6334"

    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
    name: visual-search-qdrant-storage